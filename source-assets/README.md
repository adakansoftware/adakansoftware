# Source assets

The original MacBook GLB used by `scripts/render-imported-macbook.py` is kept
locally in this directory as `macbook_pro_14_inch_M5.glb`. The model is ignored
by Git so production deployments only receive the optimized WebP frame sets.

Pass another GLB path after `--` to render from a different source model.
